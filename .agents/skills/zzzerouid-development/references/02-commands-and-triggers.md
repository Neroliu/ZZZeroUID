# 二、命令与触发器

> 返回 [SKILL.md](../SKILL.md)

均需前缀 `zzz`（或配置的可用前缀）。

| 命令 | 模块 |
|------|------|
| `绑定` / `绑定uid` / `切换` / `删除` | `zzzerouid_user` |
| `查询`（fullmatch） | `zzzerouid_roleinfo` 总览 |
| `查询<角色>` / `角色面板` / `<角色>面板` | `zzzerouid_char_detail` |
| `刷新面板` / `强制刷新` / `更新面板` | 面板刷新 |
| `练度统计` / `角色列表` | `char_list` |
| `每日` / `mr` / `便签` / `便笺` | `stamina` |
| `零号空洞` | `abyss` |
| `深渊` / `式舆防卫战` / `上期深渊` | `challenge` |
| `危局强袭战` / `强袭战` | `mem` |
| `临界推演` / `临界` | `void` |
| `抽卡记录` / `刷新抽卡记录` / `全量刷新抽卡记录` | `gachalog` |
| `绳网月报` / `札记` | `month_info` |
| `角色攻略<名>` / `<名>攻略` | `wiki`（已实现） |
| `角色图鉴` / `音擎攻略` / `驱动盘` / `武器` / `邦布` / `突破材料` | `wiki`（函数体 `pass`） |
| `兑换码` | `code` |
| `签到` / `全部重签` | `sign` |
| `帮助` | `help` |
| `下载全部资源` | `resource`（pm=2） |
| `设置…` / 开关推送 | `config` |

加命令：放到对应 `zzzerouid_*`，用已有 `get_uid` + `BIND_UID_HINT`。
尚无 `to_ai`；新命令如果希望进 Agent，按 Core 插件技能补描述。
